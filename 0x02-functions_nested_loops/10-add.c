#include "holberton.h"
/**
 * add - Is the main that returns the result
 * @k: is the parameter for the number 89 given in the main
 * @l: is the parameter for the number 9 given in the main
 * Return: the result of the addition.
 */

int add(int k, int l)
{
return (k + l);
}

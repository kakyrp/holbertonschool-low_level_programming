#include "holberton.h"
/**
* mul - returns the result of a multiplication
* using the variables a and b.
* @a: The digit to multiplicate.
* @b: the digit to multiplicate.
 * Return: the result of the multiplication
*/
int mul(int a, int b)
{
return (a * b);
}
